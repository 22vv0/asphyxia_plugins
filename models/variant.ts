export interface VariantPower {
  collection: 'variantpower';

  version: number;

  power: number,
  notes: number,
  peak: number,
  tsumami: number,
  tricky: number,
  onehand: number,
  handtrip: number,
  overRadar: numbers
}
